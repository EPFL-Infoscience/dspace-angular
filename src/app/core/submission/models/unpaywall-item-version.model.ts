/**
 * An interface to represent unpaywall version.
 */
export interface UnpaywallItemVersionModel {
  version: string;
  hostType: string;
  license?: string;
  landingPageUrl?: string;
  pdfUrl?: string;
}
