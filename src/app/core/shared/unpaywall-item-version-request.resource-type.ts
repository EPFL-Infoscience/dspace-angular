import { ResourceType } from './resource-type';

/**
 * The resource type for UnpaywallItemVersionRequest.
 *
 * Needs to be in a separate file to prevent circular
 * dependencies in webpack.
 */
export const UNPAYWALL_ITEM_VERSION_REQUEST = new ResourceType('unpaywall-item-version-request');
