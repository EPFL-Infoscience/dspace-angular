import { DsGetBundlePipe } from './ds-get-bundle.pipe';

describe('DsGetBundlePipe', () => {
  it('create an instance', () => {
    const pipe = new DsGetBundlePipe();
    expect(pipe).toBeTruthy();
  });
});
