import { GetItemsPerSetPipe } from './get-items-per-set.pipe';

describe('GetItemsPerSetPipe', () => {
  it('create an instance', () => {
    const pipe = new GetItemsPerSetPipe();
    expect(pipe).toBeTruthy();
  });
});
