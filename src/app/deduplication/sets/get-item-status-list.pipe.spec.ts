import { GetItemStatusListPipe } from './get-item-status-list.pipe';

describe('GetItemStatusListPipe', () => {
  it('create an instance', () => {
    const pipe = new GetItemStatusListPipe();
    expect(pipe).toBeTruthy();
  });
});
